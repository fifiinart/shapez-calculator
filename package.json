{
  "name": "shapez-calculator",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "live-server dist --no-dotfiles",
    "watch": "tsc --watch",
    "push": "git push && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
